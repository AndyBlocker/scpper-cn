// This plugin ensures Chart.js is only loaded on the client side
export default defineNuxtPlugin(() => {
  // Chart.js will be imported and registered in the component that uses it
  // This file just ensures proper client-side handling
})
