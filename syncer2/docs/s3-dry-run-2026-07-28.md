# S3 投票域 dry-run（2026-07-28，阻塞复验）

> 历史记录：下文是执行前快照，不代表当前状态。S3 后续已完成并封存，复算 SQL 随
> 一次性回填实现退役；原文件可从 git 历史读取。

结论：**四项阻塞均已解除，dry-run gate 全绿；仍未执行 S3。**
`ingest.vote_event=0`、`serve.vote_current=0`、`serve.page_current=0`，
且没有 `backfill_s3%` ingest run。等待人工放行，不自动进入真跑。

## 快照与安全边界

- v1 `scpper-cn`：所有查询与 top-up 源连接均以启动参数
  `default_transaction_read_only=on` 运行；dry-run 主查询使用
  `REPEATABLE READ READ ONLY`、`TimeZone=UTC`。
- v2 只发生两类获准的非事实写：S1 identity top-up，以及 1,681 条
  `meta.scan_task(kind='votes_full')` 入队；没有调用任何 S3 `apply_*`。
- 本轮功能测试中的 T7 游标用例会烧掉并清理测试专用 sequence 号，因此
  `fact_seq.last_value=1344237`；这不是 S3 分配，事实/终态/ingest-run 均为 0。
- 当时的复算主查询：`checks/backfill_s3_dryrun.sql`。

## 最终 gate

| gate | 结果 | 状态 |
|---|---:|---|
| v1 折叠基线（重跑稳定） | 32,402 / 34,083 = 95.0679% | 仅基线 |
| 外部真相种子重算 | **33,628 / 34,083 = 98.6650%** | **PASS ≥97.5%** |
| 同轮 fresh Tier1 审计 | **33,557 / 34,073 = 98.4856%** | **PASS ≥97.5%** |
| S1 identity top-up 收尾 | missing page/user/conflict = **0/0/0** | **PASS** |
| 用户偏差逐人归因 | 161 / 161；未归因 **0** | **PASS** |
| 已删页脚本化签核 | 100 / 100 | **PASS** |

### 1. live gate

1,681 页已幂等入队 `votes_full`，经代理、并发 2、M2
`src/collect/votes.ts` 实抓；完整跑发出 1,681 次 WhoRated，同轮 Tier1
给出 claimed total/rating。1,226 页四门全过，作为状态机种子；455 页四门
未过，未强行采用：

- 436 页：Wikidot WhoRated 原始 HTML 重复同一 voter、方向相同；
- 19 页：同一 voter 同时出现 `+/-`；
- unresolved identity / 其它解析问题：0 / 0。

这 455 页均是外部响应自然键重复，不是解析器漏行；逐页 claimed/fetched、
checksum 与原因见
[s3-live-partials-2026-07-28.json](s3-live-partials-2026-07-28.json)。
完整采集报告见
[s3-live-remediation-2026-07-28.json](s3-live-remediation-2026-07-28.json)。

### 2. 161 用户

9,377 人的含已删页偏差大于 5%；9,216 人由已删页口径解释，剩余 161 人：

| 分类 | 人数 |
|---|---:|
| v1 `LatestVote` 有票对、源 `Vote`/legacy 最终事件不存在 | 141 |
| CROM 票落旧 `PageVersion`、旧统计只看当前版本 | 16 |
| legacy 终态被旧 current-PV 接缝排除 | 4 |

R2/`±2`/revote-revoke 只分别伴随 1/1/7 人，不是对应票对的因果；匿名残余 0。
逐人清单与证据见
[s3-user-attribution-2026-07-28.json](s3-user-attribution-2026-07-28.json)，
说明见
[s3-user-attribution-2026-07-28.md](s3-user-attribution-2026-07-28.md)。

### 3. S1 移动靶

可重复的 `s1-topup.ts` 已补入首次发现的 4 页/3 用户并保留原 id；最终进入
S3 前再次执行为 source/target page 47,879/47,879、user
37,477/37,477，剩余 0/0。标准前置与切流收尾步骤已写入
[RUNBOOK.md](RUNBOOK.md)。

### 4. 已删页签核

固定种子、五个票数档分层抽样 100 页，并强制覆盖 C3 修正幅度最大 20 页。
样本：57 页三方全同；24 页 legacy terminal 不可得但 v1=归档；18 页
v1=legacy、差异来自归档早于 vote-resync；1 页为归档后页面变化。未归因 0。

全量归档 345 个 rating 差异进一步拆为：247 页由 legacy 反证归档旧，
45 页归档后变化，49 页 legacy terminal 不可得，4 页是
`vote_history` 终态用户未进入 `legacy_votes_cn.votes` 快照（页
95682/98577/98582/98640）。结构化表与全部 345 页见
[s3-deleted-signoff-2026-07-28.json](s3-deleted-signoff-2026-07-28.json)，
100 页签核表见
[s3-deleted-signoff-2026-07-28.md](s3-deleted-signoff-2026-07-28.md)。

## dry-run 主查询复验

R1 raw/kept/folded 仍为 6,355,765 / 1,791,473 / 4,564,292；
events 1,339,387；current nonzero 1,325,452；已删页 legacy
6,604/6,629 = 99.6229%，归档 10,893/11,238 = 96.9301%。

May-25 bootstrap raw 1,650,335 行、R1 后 140,636 行，按 Jun-1
换源规则产生事实事件仍为 **0**。因此理解确认无误：CALIBER
“bootstrap 只作累计期初余额”对本次回填数据是空操作，只对将来可能出现的
bootstrap 精度数据生效。

最终 `can_execute=true`，但 `execution_held=true`：仅表示阻塞条件已满足，
不构成执行授权。
